# MindCore Requirements
# Python 3.14 note: Some packages still have limited support

langgraph>=0.2.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-ollama>=0.2.0
gradio>=4.44.0
python-dotenv>=1.0.0
pydantic>=2.0.0
tiktoken>=0.7.0
httpx>=0.27.0
aiohttp>=3.10.0
rich>=13.0.0

# Temporarily removed for easier install on Windows + Python 3.14:
# chromadb  (needs Visual C++ Build Tools)
# crewai    (requires Python <3.14)
